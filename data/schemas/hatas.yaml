---
# Schema: hatas_operatorok.yaml
# Gyökér: hatás_operátorok (lista)

hatás_operátorok:
  - id: ""                     # kötelező, egyedi string
    név: ""                    # kötelező, string (megjelenítési név)
    mód: ""                    # kötelező, egységes effekt-mód (engine_spec §42.2). Enforce: data/gen/validators.py → validate_hatasok. Enum: "előny_hátrány" | "szorzó" | "letilt" | "max_limit" | "szöveges" | "enyhít"
