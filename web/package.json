{
  "name": "szilank-karakteralkoto",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate": "python3 ../data/generate_tables.py",
    "prebuild": "python3 ../data/generate_tables.py",
    "build": "tsc -b && vite build",
    "postbuild": "mkdir -p dist/data/tables && cp ../data/tables/*.json dist/data/tables/",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "~5.8.0",
    "vite": "^6.3.0"
  }
}
